import React from 'react';
// let iconName = "face";

// if (props.person.presence === constants.statusToBeClarified) {
//     iconName = 'perm_phone_msg';
// }
// if (props.person.presence === constants.statusAbsent) {
//     iconName = 'healing';
// }


// let txtLastChange = "keine Änderungshistorie.";

// if (undefined !== props.person.presenceLogs
//     && props.person.presenceLogs.length > 0) {
//     let logEntry = props.person.presenceLogs[0];
//     var timeStamp = new Date(logEntry.updatedAt);
//     let strDate = dateFormat(timeStamp, " 'am' dd.mm. 'um' HH:MM 'Uhr.'");
//     txtLastChange = "durch " + logEntry.user.realName + strDate;
// }

class CoordSystemContainer extends React.Component {
    render() {
        return (
            <div>my CoordSystemContainer</div>
        );
    };
}
export default CoordSystemContainer;